# Two independent projects live side by side here: web/ and admin/. Each has
# its own .gitignore; this one only covers what sits at the top level.

# uploaded media and application documents (never committed). Both projects
# point their STORAGE_LOCAL_DIR here when they run on one machine.
storage/

.DS_Store
